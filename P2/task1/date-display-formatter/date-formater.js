var executeBut = document.getSelection("");

formater = {
    format: function(){
        
    }
}
executer = {
    format: function () {
        
    }
}

executerBut.addEventListener("click", executer.format);